function checkAnswer(answer) {
    var result = document.getElementById("result");

    if (answer === "A") {
        result.innerHTML = "✅ Correct! DNA is a Double Helix.";
    } 
    else {
        result.innerHTML = "❌ Wrong answer. Try again!";
    }
}
